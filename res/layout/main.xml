<?xml version="1.0" encoding="utf-8"?>
<TabHost xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@android:id/tabhost"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:padding="5dp">
        <TabWidget
            android:id="@android:id/tabs"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content" />
        <FrameLayout
            android:id="@android:id/tabcontent"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
       		android:scrollbars="vertical"
            android:padding="5dp" >
        </FrameLayout>
    </LinearLayout>
	<RelativeLayout android:id="@+id/relative_bottom" android:layout_width="match_parent" android:layout_height="wrap_content" android:layout_gravity="bottom|center">
        <Button android:layout_width="wrap_content" android:id="@+id/button_reboot" android:layout_height="wrap_content" android:text="Reboot"></Button>
        <Button android:text="Close" android:layout_width="wrap_content" android:id="@+id/button_close" android:layout_height="wrap_content" android:layout_toRightOf="@+id/button_reboot"></Button>
    </RelativeLayout>
</TabHost>
